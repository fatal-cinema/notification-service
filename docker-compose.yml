services:
  notification-service:
    container_name: fatal-cinema-notification-service
    image: fatal-cinema/notification-service:dev
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    env_file:
      - .env.production.local
    networks:
      - fatal-cinema

networks:
  fatal-cinema:
    external: true
